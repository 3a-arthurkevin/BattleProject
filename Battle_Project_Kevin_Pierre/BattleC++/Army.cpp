#include "StdAfx.h"
#include "Army.h"

//#include "Vector.h"
#include "targetver.h"

#include "Unit.h"

Army::~Army(void)
{
}

Army::Army(void)
{
	/*
	private:
		std::vector<Unit*> _units;

	public:
		Army(int nbUnit, int globalLevel);
		Army(std::vector<Unit> & units);

		std::vector<Unit*> getUnitsList();
		
		Unit& getUnit(int id);
		
		int size();

		Unit& getNearestUnit(const Point& point);
		Unit& getFurtherUnit(const Point& point);
		Unit& getLowestUnit(int capacityIndex);
		Unit& getHighestUnit(int capacityIndex);

		void purge();
	*/
}
